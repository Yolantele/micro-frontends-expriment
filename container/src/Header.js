import React from 'react';
import './Header.css';

const Header = () => (
  <header>
    <div className="center-column">
      <h1>Container / Main App Header</h1>
    </div>
  </header>
);

export default Header;
